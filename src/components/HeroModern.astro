---
---

<section class="relative min-h-screen flex items-center justify-center overflow-hidden bg-gradient-to-b from-gray-50 to-white pt-20">
  <!-- Background Geometric Pattern -->
  <div class="absolute inset-0 opacity-5">
    <div class="absolute inset-0" style="background-image: linear-gradient(rgba(0,0,0,0.05) 1px, transparent 1px), linear-gradient(90deg, rgba(0,0,0,0.05) 1px, transparent 1px); background-size: 50px 50px;"></div>
  </div>

  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-20 relative z-10">
    <div class="grid lg:grid-cols-2 gap-12 items-center">
      <!-- Content -->
      <div class="space-y-8 animate-fade-in">
        <div class="inline-flex items-center gap-2 bg-gray-900 text-white px-4 py-2 text-xs font-semibold tracking-widest uppercase animate-slide-down">
          <span class="material-symbols-outlined text-sm">verified</span>
          <span>+8 Años de experiencia</span>
        </div>

        <h1 class="text-5xl sm:text-6xl lg:text-7xl font-bold text-gray-900 leading-tight animate-slide-up">
          Diseñamos
          <span class="block text-gray-600 mt-2">Espacios que</span>
          <span class="block mt-2">Inspiran</span>
        </h1>

        <p class="text-lg sm:text-xl text-gray-600 leading-relaxed max-w-xl animate-fade-in-delayed">
          Transformamos visiones en realidad arquitectónica. Cada proyecto es una obra de arte funcional que refleja la identidad y necesidades de nuestros clientes.
        </p>

        <div class="flex flex-col sm:flex-row gap-4 animate-slide-up-delayed">
          <a href="/proyectos" class="btn-primary group">
            <span>Ver Proyectos</span>
            <span class="material-symbols-outlined text-lg transform group-hover:translate-x-1 transition-transform">arrow_forward</span>
          </a>
          <a href="/contacto" class="btn-secondary group">
            <span>Contactar</span>
            <span class="material-symbols-outlined text-lg">mail</span>
          </a>
        </div>

        <!-- Stats -->
        <div class="grid grid-cols-3 gap-8 pt-8 border-t border-gray-200 animate-fade-in-delayed">
          <div>
            <div class="text-3xl sm:text-4xl font-bold text-gray-900">30+</div>
            <div class="text-sm text-gray-600 mt-1">Proyectos</div>
          </div>
          <div>
            <div class="text-3xl sm:text-4xl font-bold text-gray-900">100%</div>
            <div class="text-sm text-gray-600 mt-1">Satisfacción</div>
          </div>
          <div>
            <div class="text-3xl sm:text-4xl font-bold text-gray-900">8+</div>
            <div class="text-sm text-gray-600 mt-1">Años</div>
          </div>
        </div>
      </div>

      <!-- Image -->
      <div class="relative animate-fade-in-right">
        <div class="relative z-10">
          <img 
            src="https://images.unsplash.com/photo-1600585154340-be6161a56a0c?w=800&h=1000&fit=crop" 
            alt="Modern Architecture" 
            class="w-full h-auto rounded-lg shadow-2xl"
          />
        </div>
        <!-- Decorative elements -->
        <div class="absolute -top-4 -left-4 w-32 h-32 bg-gray-900 rounded-lg -z-10"></div>
        <div class="absolute -bottom-4 -right-4 w-32 h-32 bg-gray-200 rounded-lg -z-10"></div>
      </div>
    </div>
  </div>

  <!-- Scroll Indicator -->
  <div class="absolute bottom-8 left-1/2 transform -translate-x-1/2 animate-bounce">
    <span class="material-symbols-outlined text-4xl text-gray-400">expand_more</span>
  </div>
</section>

<style>
  .btn-primary {
    @apply inline-flex items-center gap-2 bg-gray-900 text-white px-8 py-4 text-sm font-semibold tracking-wide uppercase transition-all duration-300 hover:bg-gray-800 hover:shadow-xl;
  }

  .btn-secondary {
    @apply inline-flex items-center gap-2 bg-white text-gray-900 px-8 py-4 text-sm font-semibold tracking-wide uppercase border-2 border-gray-900 transition-all duration-300 hover:bg-gray-900 hover:text-white;
  }

  .material-symbols-outlined {
    font-variation-settings:
    'FILL' 0,
    'wght' 300,
    'GRAD' 0,
    'opsz' 24
  }

  @keyframes fadeIn {
    from {
      opacity: 0;
      transform: translateY(20px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  @keyframes slideUp {
    from {
      opacity: 0;
      transform: translateY(30px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  @keyframes slideDown {
    from {
      opacity: 0;
      transform: translateY(-20px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  @keyframes fadeInRight {
    from {
      opacity: 0;
      transform: translateX(30px);
    }
    to {
      opacity: 1;
      transform: translateX(0);
    }
  }

  .animate-fade-in {
    animation: fadeIn 0.8s ease-out;
  }

  .animate-fade-in-delayed {
    animation: fadeIn 1s ease-out 0.2s both;
  }

  .animate-slide-up {
    animation: slideUp 0.8s ease-out 0.1s both;
  }

  .animate-slide-up-delayed {
    animation: slideUp 0.8s ease-out 0.3s both;
  }

  .animate-slide-down {
    animation: slideDown 0.6s ease-out;
  }

  .animate-fade-in-right {
    animation: fadeInRight 1s ease-out 0.4s both;
  }
</style>
